# -------------------------------------------- #
#               APP依赖定义                     #
# -------------------------------------------- #
# 安装包版本。必填。固定值
apiVersion: v3alpha1
# 依赖项
#dependencies:
#  # OpenAPI依赖项。必填。
#  - name: openapi
#    # 模块列表
#    modules:
#      # 模块名称。必填。
#      - name: auth2
#        # 版本。必填。格式需要符合[SemVer 2](https://semver.org/)规范。如:1.0.0
#        version: 1.0.0
#        # 版本区间。必填。参考[maven 的version range标准定义](http://maven.apache.org/enforcer/enforcer-rules/versionRanges.html)
#        # 当设置版本区间时，version可不必填。
#        versionRange: "[1.0.0,3.0.0)"
#    # APP依赖项。
#  - name: app
#    # APP列表
#    modules:
#      # APP名称。必填。格式为<vendorName>-<appName>
#      - name: vendorName-appName
#        # 版本。必填。格式需要符合[SemVer 2](https://semver.org/)规范。如:1.0.0
#        version: 1.0.0
#        # 版本区间。必填。参考[maven 的version range标准定义](http://maven.apache.org/enforcer/enforcer-rules/versionRanges.html)
#        # 当设置版本区间时，version可不必填。
#        versionRange: "[1.0.0,3.0.0)"