# -------------------------------------------- #
#               开发者定义                      #
# -------------------------------------------- #
# 安装包版本。必填。固定值
apiVersion: v3alpha1
vendor:
  # 开发者显示名称。必填。
  showName: default
  # app运营平台（BOSS）的账号ID。必填。
  bluetronId: default
#  # 开发者组织名称。非必填。
#  organization:
#  # 开发者组织的网址。非必填。
#  url:
#  # 权限声明。非必填。
#  copyright:
#  # 开发者组织的地址。非必填。
#  address:
#  # 开发者组织的邮箱地址。必填。方便问题反馈。
#  email: